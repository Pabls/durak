<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Title" parent="AppTheme">
        <item name="android:textSize">@dimen/title</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.PrimaryButton" parent="Base.TextAppearance.MaterialComponents.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@color/colorAccent</item>
        <item name="android:padding">@dimen/padding_8</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="AppTheme.Statistics.Label" parent="AppTheme.Title">
        <item name="android:textSize">@dimen/statistics_activity.label</item>
        <item name="android:layout_marginStart">@dimen/margin_8</item>
        <item name="android:layout_marginTop">@dimen/margin_32</item>
    </style>

    <style name="AppTheme.Statistics.Data" parent="AppTheme.Statistics.Label">
        <item name="android:textSize">@dimen/statistics_activity.label</item>
        <item name="android:layout_marginStart">@dimen/margin_8</item>
        <item name="android:layout_width">@dimen/match_constraint</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>
